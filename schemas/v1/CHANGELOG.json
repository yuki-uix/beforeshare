{
  "$comment": "Machine-readable changelog for the canonical schemas (§13.3 requires one alongside the machine-readable schema). Each change is classified, and tools/versioning.mjs decides from these classifications whether a version bump was legal — so a breaking change recorded under an unchanged MAJOR fails the build rather than shipping.",
  "schemaVersion": "1.0",
  "releases": [
    {
      "version": "1.0",
      "date": "2026-09-14",
      "changes": [
        {
          "type": "additive",
          "target": "inspection-result.schema.json",
          "description": "Initial canonical inspection result."
        },
        {
          "type": "additive",
          "target": "verification-result.schema.json",
          "description": "Initial verification result."
        },
        {
          "type": "additive",
          "target": "capabilities.schema.json",
          "description": "Initial capability declaration."
        },
        {
          "type": "additive",
          "target": "enums.schema.json",
          "description": "limitationCode gains ocr_not_available. A coverage-reducing skip must be named by a limitation, and the nearest existing codes are answers from an OCR that ran - a build with no OCR could not say what it lacked."
        }
      ]
    }
  ]
}
